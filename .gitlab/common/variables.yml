# GitLab CI common variables for Dockflow
# The CLI is installed at runtime, no need for custom images

.common-variables:
  image: ubuntu:22.04
  services:
    - docker:dind
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""