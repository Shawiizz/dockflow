include:
  - remote: 'https://raw.githubusercontent.com/Shawiizz/dockflow/2.0.9/.gitlab/common/steps.yml'
  - remote: 'https://raw.githubusercontent.com/Shawiizz/dockflow/2.0.9/.gitlab/common/variables.yml'

stages:
  - deploy

deploy-branch:
  stage: deploy
  rules:
    - if: '$CI_COMMIT_BRANCH'
  extends:
    - .common-variables
    - .prepare-env
    - .deploy-with-dockflow
