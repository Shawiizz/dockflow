services:
  test-vm-manager:
    build:
      context: ..
      dockerfile: docker/Dockerfile.test-vm
    container_name: dockflow-test-manager
    hostname: dockflow-test-mgr
    privileged: true
    environment:
      - SWARM_ROLE=manager
    ports:
      - "32222:22"
      - "38080:80"
      # e2e registry (runs inside this DinD node, published in cascade so
      # `localhost:35000` works from both the host and the inner daemon)
      - "35000:35000"
    volumes:
      - swarm-data:/swarm
      - /etc/ssl/certs:/etc/ssl/certs:ro
    networks:
      - test-network
    healthcheck:
      test: ["CMD", "bash", "/healthcheck.sh"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 15s

  test-vm-worker-1:
    build:
      context: ..
      dockerfile: docker/Dockerfile.test-vm
    container_name: dockflow-test-worker-1
    hostname: dockflow-test-w1
    privileged: true
    environment:
      - SWARM_ROLE=worker
      - SWARM_MANAGER_HOST=dockflow-test-mgr
    ports:
      - "32223:22"
    volumes:
      - swarm-data:/swarm
      - /etc/ssl/certs:/etc/ssl/certs:ro
    networks:
      - test-network
    depends_on:
      test-vm-manager:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "bash", "/healthcheck.sh"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 15s

volumes:
  swarm-data:
    driver: local

networks:
  test-network:
    driver: bridge
